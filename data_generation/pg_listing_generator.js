const fs = require('fs');
const faker = require('faker');

const createListing = () => {
  const listing = faker.productName();
  return `${listing}\n`;
};








// for num of listings times
  // for variable num of reviews
    // create review

// for num of users times
  // create user